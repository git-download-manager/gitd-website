# Log SERVICENAME generated log messages to file
:programname, isequal, "SERVICENAME" /var/log/gitdm/SERVICENAME.log

# comment out the following line to allow SERVICENAME messages through.
# Doing so means you'll also get SERVICENAME messages in /var/log/syslog
& stop